# 安全说明

authx 面向工业级生产使用，安全策略如下。

## 报告漏洞

请通过 GitHub Issues 私密或公开提交漏洞报告，并包含：

- 影响的版本与包（password/token/rbac/middleware/session/mfa/oauth2/audit/security）；
- 可复现的最小代码片段或输入；
- 预期行为与实际行为的差异。

确认后将在下一版本修复并记录到 CHANGELOG。若为高危漏洞，会尽快发布
修复版本并建议升级。

## 支持范围

- 始终支持最新版本（当前版本线为 v1.x；破坏性变更按家族约定走 minor
  版本，见 CHANGELOG）；
- 历史版本仅在发现高危漏洞时评估修复，建议生产环境升级到最新版本；
- 内存存储（MemoryStore 系列）仅适用于单实例场景；多实例部署请
  自行实现 Redis 等外部存储接口。

## 安全基线

- 密码哈希：Argon2id（RFC 9106），默认 OWASP 参数，含恶意哈希参数上限；
- 令牌：JWT 全套算法、常量时间语义、撤销列表、刷新令牌哈希落库与轮换；
- 会话：随机 ID、深拷贝隔离、TTL 过期、容量上限、登录后轮换防固定；
- TOTP：RFC 6238（SHA1/256/512、6/8 位、自定义周期），skew 有上限；
- 防爆破：滑动窗口失败计数、锁定、条目容量上限；
- 输入防御：所有解析入口有长度/规模上限，随机源失败返回错误而非弱回退；
- 审计钩子 panic 隔离，日志字段限长。

## 安全实践建议

- 生产环境必须启用 HTTPS，Cookie 使用 Secure + HttpOnly + SameSite；
- 密钥（JWT 私钥、HMAC 密钥、客户端密钥）使用密钥管理系统保管，
  通过环境变量或配置中心注入，切勿写入代码仓库；
- 定期轮换签名密钥（支持 kid 多密钥验证）；
- 多实例部署使用共享存储实现会话/刷新令牌/撤销列表；
- 定期执行 `govulncheck ./...` 检查依赖漏洞（CI 已内置）。
